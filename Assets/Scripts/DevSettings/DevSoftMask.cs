using UnityEngine;

namespace PeopleFun.UI
{
    /// <summary>
    /// Placeholder for the imported prefab's soft-mask component. The scroll view
    /// already clips with a built-in <c>RectMask2D</c>, so no behaviour is needed;
    /// this exists only so the prefab does not import as a missing script.
    /// </summary>
    public class DevSoftMask : MonoBehaviour
    {
    }
}
